<h2>注文履歴詳細</h2>

<%= @order.each do |order| %>
          <td>
            <%= form_with model: order, url: admin_order_path(order), method: :patch do |f| %>
            <%= f.serect :status, Order.statuses, class: "status_serect" %>
            <%= f.submit "">
          </td>
